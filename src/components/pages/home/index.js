import Container from '@mui/material/Container';
import FiltersBar from './FiltersBar';

const Home = () => {
   return (
      <Container maxWidth="xl">
         <FiltersBar />
      </Container>
   );
};

export default Home;
